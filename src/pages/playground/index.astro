---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import StubBody from '../../components/StubBody.astro';
---

<BaseLayout
  title="Srenix &mdash; Playground"
  description="The hosted Srenix playground is coming: a pre-provisioned kind cluster + LocalStack AWS sub-account with synthetic drift on a 30-second cycle. Until it ships, the quick-start gets Srenix running in your own cluster in 5 minutes."
>
  <PageHero
    eyebrow="Playground"
    title="Try Srenix in a hosted sandbox."
    subtitle="Coming soon: a pre-provisioned cloud sandbox with synthetic drift — no local tooling needed. Today: a local kind-cluster quick-try (docker + kubectl required)."
  />
  <StubBody
    claim="The hosted playground is coming. It will run a pre-provisioned kind cluster + LocalStack AWS sub-account (RDS, EBS, IAM, ALB, ACM) with synthetic drift injected on a 30-second cycle. You'll see the live DriftReport stream + Slack feed in an iframe, with a single CTA: 'Run this in your own cluster' → helm install."
  />
</BaseLayout>
