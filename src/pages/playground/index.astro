---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import StubBody from '../../components/StubBody.astro';
---

<BaseLayout
  title="CHA &mdash; Playground"
  description="The hosted CHA playground is coming: a pre-provisioned kind cluster + LocalStack AWS sub-account with synthetic drift on a 30-second cycle. Until it ships, the quick-start gets CHA running in your own cluster in 5 minutes."
>
  <PageHero
    eyebrow="Playground"
    title="Try CHA in a hosted sandbox."
    subtitle="A pre-provisioned kind cluster + LocalStack AWS sub-account with synthetic drift on a 30-second cycle. Watch a DriftReport get opened, fixed, and re-verified — without installing anything."
  />
  <StubBody
    claim="The hosted playground is coming. It will run a pre-provisioned kind cluster + LocalStack AWS sub-account (RDS, EBS, IAM, ALB, ACM) with synthetic drift injected on a 30-second cycle. You'll see the live DriftReport stream + Slack feed in an iframe, with a single CTA: 'Run this in your own cluster' → helm install."
  />
</BaseLayout>
