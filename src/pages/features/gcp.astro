---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import StubBody from '../../components/StubBody.astro';
---

<BaseLayout
  title="CHA &mdash; GCP probes (roadmap M2 / v1.7+)"
  description="GCP probes covering Cloud SQL, GKE, IAM, Load Balancers, GCS, KMS — scoped for M2 (v1.7+). Workload Identity native."
>
  <PageHero
    eyebrow="Features &middot; GCP (roadmap)"
    title="GCP probes — roadmap M2 (v1.7+)."
    subtitle="Cloud SQL, GKE, IAM, Load Balancers, GCS, KMS. The pkg/cloud/gcp client is scaffolded today; the probe set ships in v1.7+, modelled on the AWS IRSA pattern already live."
  />
  <StubBody
    claim="GCP cloud probes are roadmap M2 (v1.7+). The pkg/cloud/gcp/client.go scaffold is in the OSS repo today; the probe implementations (modelled on the 10 AWS probes that shipped in v1.6.2) land in v1.7+. CHA on GKE will use Workload Identity — the cluster's GCP service-account binding — to probe Cloud SQL, GKE control-plane state, IAM, Load Balancers, GCS, KMS, and more. Zero long-lived credentials. Read-only by design."
    backHref="/features/"
    backLabel="← All features"
  />
</BaseLayout>
