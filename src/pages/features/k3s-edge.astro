---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import StubBody from '../../components/StubBody.astro';
---

<BaseLayout
  title="Srenix &mdash; k3s + edge"
  description="Reduced-footprint values bundle for single-node edge appliances. Same product, on a 2-CPU SBC."
>
  <PageHero
    eyebrow="Features &middot; k3s + edge"
    title="The same product, on a 2-CPU edge appliance."
    subtitle="examples/k3s-edge/ ships a reduced-footprint Helm values bundle so Srenix runs on single-node edge appliances and full-fat EKS clusters from the same chart."
  />
  <StubBody
    claim="Air-gapped, sovereign-deployable, single-node edge — Srenix is the only product in this category that ships a values bundle tuned for k3s on a 2-CPU SBC. The K8s-native value (probes, fixers, ticketing) is unchanged. Set cloud.enabled=false to skip cloud probes cleanly."
    backHref="/features/"
    backLabel="← All features"
  />
</BaseLayout>
